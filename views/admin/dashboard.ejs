<div>
  <div class="admin-title">
    <h2>Konular</h2>
    <a href="/add-post" class="button">+ Yeni Ekle</a>
  </div>

  <ul class="admin-posts">
    <% data.forEach(post => { %>

    <li>
      <div style="width: 50%">
        <a href="/post/<%= post._id %>"> <%= post.title %> &nearr; </a>
      </div>
      <div class="admin-post-controls">
        <a href="/edit-post/<%= post._id %>" class="btn">Düzenle</a>

        <form
          action="/delete-post/<%= post._id %>?_method=DELETE"
          method="POST"
        >
          <input type="submit" value="Sil" class="btn-delete btn" />
        </form>
      </div>
    </li>

    <% }) %>
  </ul>
</div>
